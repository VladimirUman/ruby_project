$('#notice').hide();

if ($('#cart tr').length != 0) { $('#cartModal').modal('show'); }

if ($('#cart tr').length != 0) { $('#cart-quantity').show('blind', 100); }

$('#cart').html("<%=j render(@cart) %>");

$('#cart-quantity').html("<%= @cart.total_quantity %>");
