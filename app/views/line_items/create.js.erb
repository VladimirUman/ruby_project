$('#notice').hide();

if ($('#cart tr').length != 0) { $('#cartModal').modal('show'); }

if ($('#cart tr').length != 0) { $('#korz').show('blind', 100); }

$('#cart').html("<%=j render(@cart) %>");

$('#korz').html("<%= @cart.total_quantity %>");
